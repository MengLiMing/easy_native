name: easy_native
description: "A new Flutter plugin project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.0.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  result_dart: ^2.2.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.easy_native
        pluginClass: EasyNativePlugin
      ios:
        pluginClass: EasyNativePlugin
